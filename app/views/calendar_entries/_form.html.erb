<%= form_for(@calendar_entry) do |f| %>
  <% if @calendar_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@calendar_entry.errors.count, "error") %> prohibited this calendar_entry from being saved:</h2>

      <ul>
      <% @calendar_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :text %><br />
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
